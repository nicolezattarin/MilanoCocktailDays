---
/**
 * CtaBanner — sezione CTA finale ripetuta in fondo a ogni pagina.
 * variant 'borgogna'/'bitter': sfondo Bitter, bottone Panna.
 * variant 'dark': sfondo Mora, bottone Bitter.
 */
interface Props {
  title: string;
  ctaText: string;
  ctaHref: string;
  variant?: 'borgogna' | 'dark';
  subtitle?: string;
  ctaClass?: string;
}

const { title, ctaText, ctaHref, variant = 'borgogna', subtitle, ctaClass } = Astro.props;
---

<section
  class:list={['cta-banner mcw-dot-grid', `cta-banner--${variant}`]}
  aria-labelledby="cta-banner-heading"
>
  <div class="wrap">
    <span class="cta-star" aria-hidden="true">✶</span>
    <h2 id="cta-banner-heading" class="cta-title">{title}</h2>
    {subtitle && <p class="cta-sub">{subtitle}</p>}
    <a href={ctaHref} class:list={['cta-btn', ctaClass]}>{ctaText}</a>
  </div>
</section>

<style>
  .cta-banner {
    padding: var(--section-py) 0;
  }

  /* borgogna variant (backward compat) = bitter variant */
  .cta-banner--borgogna,
  .cta-banner--bitter {
    background-color: var(--color-bitter);
  }

  .cta-banner--dark {
    background-color: var(--color-mora);
  }

  .wrap {
    max-width: var(--container);
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-sm);
  }

  .cta-star {
    color: var(--color-panna);
    font-size: 1.8rem;
    opacity: 0.5;
  }

  .cta-title {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h1);
    line-height: 1.05;
  }

  .cta-banner--borgogna .cta-title,
  .cta-banner--bitter .cta-title {
    color: var(--color-panna);
  }

  .cta-banner--dark .cta-title {
    color: var(--color-panna);
  }

  .cta-sub {
    font-family: var(--font-body);
    font-size: var(--text-body);
    line-height: 1.65;
    max-width: 480px;
    margin: 0;
    opacity: 0.85;
  }

  .cta-banner--borgogna .cta-sub,
  .cta-banner--bitter .cta-sub {
    color: var(--color-panna);
  }

  .cta-banner--dark .cta-sub {
    color: var(--color-pesco-300);
  }

  /* Bottone: Panna su Bitter; Bitter su Mora */
  .cta-btn {
    display: inline-flex;
    align-items: center;
    padding: 0.875rem 2rem;
    font-family: var(--font-body);
    font-size: var(--text-small);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    text-decoration: none;
    border-radius: var(--radius-sm);
    transition: background-color 0.2s ease, color 0.2s ease, border-color 0.2s ease;
    margin-top: var(--space-xs);
  }

  .cta-banner--borgogna .cta-btn,
  .cta-banner--bitter .cta-btn {
    background-color: var(--color-panna);
    color: var(--color-bitter);
    border: 1px solid var(--color-panna);
  }

  .cta-banner--borgogna .cta-btn:hover,
  .cta-banner--bitter .cta-btn:hover {
    background-color: transparent;
    color: var(--color-panna);
  }

  .cta-banner--dark .cta-btn {
    background-color: var(--color-bitter);
    color: var(--color-panna);
    border: 1px solid var(--color-bitter);
  }

  .cta-banner--dark .cta-btn:hover {
    background-color: var(--color-bitter-500);
    border-color: var(--color-bitter-500);
    color: var(--color-panna);
  }
</style>
