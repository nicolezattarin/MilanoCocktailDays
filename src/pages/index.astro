---
import BaseLayout from '../layouts/BaseLayout.astro';

const steps = [
  {
    n: '01',
    title: 'Prendi il pass',
    desc: "Uno solo, valido per i dieci giorni di maggio. I primi pass partono da €15, poi €30. Si compra una volta sola.",
  },
  {
    n: '02',
    title: 'Scegli i bar',
    desc: "Trenta locali da Brera a NoLo. Tu decidi il ritmo, il quartiere, la compagnia. Nessuna prenotazione, nessun itinerario obbligato.",
  },
  {
    n: '03',
    title: 'Bevi il Cocktail Days',
    desc: "Ogni bar inventa il suo, e lo serve solo in questi dieci giorni. €6 l'uno, sempre — qualunque sia il bar, qualunque sia l'ora. Trenta ricette che non bevi da nessun'altra parte, fatte apposta per la settimana.",
  },
];

const criteria = [
  {
    title: 'Cocktail come mestiere.',
    desc: 'La miscelazione prima del concept, della musica, dei selfie.',
  },
  {
    title: 'Quartieri vivi.',
    desc: 'Da Brera a Lambrate, dai Navigli a NoLo. Milano non è solo il centro.',
  },
  {
    title: 'Voci della scena.',
    desc: "Non nomi su un'insegna: persone con cui si parla.",
  },
];

const appFeatures = [
  {
    title: 'Acquista il biglietto.',
    desc: "Acquista il tuo biglietto e ottieni l'accesso all'app con tutti gli oltre 60 bar partecipanti e i loro cocktail del festival. Il tuo biglietto ti accompagnerà per 18 giorni durante tutto il festival.",
    wide: false,
  },
  {
    title: 'Scopri i bar e i cocktail.',
    desc: "Nell'app puoi vedere quali bar partecipano e quale signature cocktail puoi sbloccare lì. Ogni bar crea un cocktail unico, disponibile solo durante il festival.",
    wide: false,
  },
  {
    title: 'Scansiona e ordina.',
    desc: "Scansiona il codice QR al bar con l'app, sblocca il signature cocktail e ordinalo direttamente sul posto a 6 €.",
    wide: true,
  },
];

const quotes = [
  { text: 'Finalmente un evento come la London Cocktail Week a Milano!', name: 'Giulia', age: 25 },
  { text: 'Cocktail a €6 è già abbastanza per comprare il biglietto', name: 'Marco', age: 24 },
  { text: 'Una scusa per uscire durante la settimana a Milano', name: 'Elena', age: 30 },
  { text: 'Sembra un modo carino per scoprire nuovi bar', name: 'Luca', age: 34 },
];

const faqs = [
  {
    q: 'Iscrivermi alla lista significa che ho già comprato il pass?',
    a: "No. La lista d'attesa è solo un'email-promemoria. Quando aprono i pass, ti scriviamo per primi.",
  },
  {
    q: 'Quanto costa il pass?',
    a: 'I primi (super early bird) partono da €15. Quando finiscono, il prezzo standard è €30.',
  },
  {
    q: "Quando esattamente si svolge l'evento?",
    a: 'Dieci giorni di maggio 2027. Le date precise le annunciamo a chi è in lista, prima di tutti.',
  },
  {
    q: "Cos'è il Cocktail Days? È un cocktail scontato?",
    a: "No. È un cocktail che il bar inventa apposta per la settimana — una ricetta nuova, servita solo nei dieci giorni dell'evento, allo stesso prezzo in tutti i trenta bar: €6.",
  },
  {
    q: 'Cosa è incluso nel pass?',
    a: "L'accesso ai trenta bar partner per l'intera durata dell'evento, e il diritto a ordinare il Cocktail Days a €6 — in ogni bar, quante volte vuoi.",
  },
  {
    q: 'Devo prenotare nei bar?',
    a: 'No. Entri quando vuoi, mostri il pass, ordini.',
  },
  {
    q: "Serve l'app?",
    a: "No. Il pass basta. L'app è un compagno di strada, non un requisito.",
  },
  {
    q: 'Posso regalare o trasferire il pass?',
    a: "Stiamo lavorando alla regalistica. I dettagli arrivano all'apertura delle vendite — anche su questo, chi è in lista lo sa per primo.",
  },
];

const milanDots = [
  { x: 175, y: 130 }, { x: 185, y: 142 }, { x: 170, y: 150 },
  { x: 196, y: 112 }, { x: 212, y: 120 },
  { x: 242, y: 118 }, { x: 256, y: 128 }, { x: 250, y: 108 },
  { x: 158, y: 232 }, { x: 170, y: 246 }, { x: 162, y: 220 },
  { x: 215, y: 258 }, { x: 226, y: 268 }, { x: 234, y: 250 },
  { x: 290, y: 180 }, { x: 302, y: 194 }, { x: 285, y: 167 },
  { x: 188, y: 158 }, { x: 200, y: 162 },
  { x: 136, y: 176 }, { x: 128, y: 166 }, { x: 142, y: 190 },
  { x: 252, y: 162 }, { x: 264, y: 172 },
  { x: 154, y: 92  }, { x: 168, y: 102 },
  { x: 205, y: 198 }, { x: 216, y: 186 },
  { x: 266, y: 146 }, { x: 200, y: 215 },
];
---

<BaseLayout
  title="Milano Cocktail Days — Trenta bar, dieci giorni, un pass."
  description="Maggio 2027: trenta bar di Milano, dieci giorni, un cocktail inventato apposta in ogni bancone a €6. Mettiti in lista per i primi pass a €15."
  pageType="event"
>

  <!-- S01 · HERO ─────────────────────────────────────────────── -->
  <section class="s-hero" aria-labelledby="home-h1">
    <picture class="hero-bg" aria-hidden="true">
      <img
        src="/images/bar_photo1.jpg"
        alt=""
        width="1920" height="1080"
        fetchpriority="high" loading="eager"
      />
    </picture>
    <div class="hero-overlay" aria-hidden="true"></div>

    <div class="wrap hero-inner">
      <p class="hero-overline">
        Milano <span aria-hidden="true">✶</span> Maggio 2027 <span aria-hidden="true">✶</span> Prima Edizione
      </p>

      <div class="hero-h1-wrap mcw-bracket">
        <h1 id="home-h1" class="hero-h1">
          Trenta bar di Milano.<br />Per dieci giorni.<br />Un pass.
        </h1>
      </div>

      <p class="hero-sub">
        Ogni bar inventa un cocktail solo per te. €6 l'uno, solo nei dieci giorni di maggio.
        Mettiti in lista — i primi pass partono da €15.
      </p>

      <a href="#lista" class="btn btn--primary">
        Entra in lista d'attesa <span aria-hidden="true">✶</span>
      </a>
    </div>
  </section>

  <!-- S02 · COME FUNZIONA ────────────────────────────────────── -->
  <section class="s-how" aria-labelledby="how-h2">
    <div class="wrap">
      <h2 id="how-h2" class="how-h2">Tre cose, niente di più.</h2>

      <ol class="step-list" role="list">
        {steps.map(({ n, title, desc }) => (
          <li class="step">
            <div class="step-header">
              <span class="step-n" aria-hidden="true">{n}</span>
              <span class="step-bullet" aria-hidden="true">✶</span>
              <h3 class="step-title">{title}</h3>
            </div>
            <p class="step-desc">{desc}</p>
          </li>
        ))}
      </ol>

      <p class="how-strap">
        Trenta bar. Trenta cocktail unici. Disponibili solo dentro MCD.
        <span aria-hidden="true">✶</span>
      </p>
    </div>
  </section>

  <!-- S03 · PERCHÉ ISCRIVERSI ────────────────────────────────── -->
  <section class="s-why" aria-labelledby="why-h2">
    <div class="wrap">
      <h2 id="why-h2" class="why-h2">I primi a saperlo. I primi a entrare.</h2>

      <div class="why-grid">
        <div class="why-block">
          <h3 class="why-block-title">Priorità.</h3>
          <p class="why-block-body">
            Quando i pass aprono, la prima email arriva a chi è in lista.
            Senza coda, senza refresh, senza dover essere svegli all'ora giusta.
          </p>
        </div>
        <div class="why-block">
          <h3 class="why-block-title">
            Super early bird a <span class="price">€15</span>.
          </h3>
          <p class="why-block-body">
            I primi pass costano la metà del prezzo standard. Sono limitati:
            quando finiscono, si passa a €30. Niente trucchi, solo conti.
          </p>
        </div>
      </div>

      <p class="why-counter" aria-live="polite">
        <strong class="counter-num" id="counter">347</strong> persone già in lista
      </p>

      <a href="#lista" class="btn btn--primary">
        Entra in lista d'attesa <span aria-hidden="true">✶</span>
      </a>
    </div>
  </section>

  <!-- S05 · PER CHI È ────────────────────────────────────────── -->
  <section class="s-perchi" aria-labelledby="perchi-h2">
    <div class="wrap">
      <h2 id="perchi-h2" class="perchi-h2">
        Per chi ama Milano. E per chi vuole conoscerla meglio.
      </h2>

      <p class="perchi-lead">
        MCD non è per gli addetti ai lavori. È per chi al bancone ci sta bene,
        sa o non sa quello che ordina, e ha voglia di fare un giro che valga la pena.
      </p>

      <ul class="perchi-occasions" role="list">
        <li>Per il sabato pomeriggio con chi ti piace.</li>
        <li>Per una serata diversa con gli amici di sempre.</li>
        <li>Per chi è curioso e non sa da dove iniziare.</li>
      </ul>

      <p class="perchi-close">
        Nessun dress code. Nessun esame. Solo bicchieri e bar che vale la pena conoscere.
        <span aria-hidden="true">✶</span>
      </p>
    </div>
  </section>

  <!-- S06a · I BAR PARTNER ───────────────────────────────────── -->
  <section class="s-bars" aria-labelledby="bars-h2">
    <div class="wrap">
      <h2 id="bars-h2" class="bars-h2">Non tutti i bar di Milano. I trenta che contano.</h2>

      <p class="bars-lead">
        Stiamo costruendo la mappa. Bar indipendenti, bartender con un'idea, cocktail che
        parlano. Quando i primi nomi saranno sul tavolo, chi è in lista li legge per primo.
      </p>

      <div class="criteria-grid">
        {criteria.map(({ title, desc }) => (
          <div class="criterion">
            <h3 class="criterion-title">{title}</h3>
            <p class="criterion-desc">{desc}</p>
          </div>
        ))}
      </div>

      <figure class="map-wrap">
        <svg
          viewBox="0 0 400 360"
          xmlns="http://www.w3.org/2000/svg"
          class="milan-map"
          aria-hidden="true"
          role="presentation"
        >
          <path
            d="M 80 120 Q 110 65 170 55 Q 225 46 285 68 Q 345 88 362 145 Q 378 185 360 245 Q 340 295 278 315 Q 218 335 158 315 Q 98 294 80 248 Q 62 195 80 120 Z"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            opacity="0.25"
          />
          {milanDots.map(({ x, y }) => (
            <circle
              cx={x}
              cy={y}
              r="5"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              opacity="0.65"
            />
          ))}
        </svg>
        <figcaption class="map-caption">
          In aggiornamento — i primi nomi a giorni. <span aria-hidden="true">✶</span>
        </figcaption>
      </figure>

      <a href="#lista" class="btn btn--primary">
        Iscriviti per saperlo prima di tutti <span aria-hidden="true">✶</span>
      </a>
    </div>
  </section>

  <!-- S06b · L'APP ───────────────────────────────────────────── -->
  <section class="s-app" aria-labelledby="app-h2">
    <div class="wrap app-inner">
      <div class="app-mock" aria-hidden="true">
        <div class="phone-frame">
          <div class="phone-notch"></div>
          <div class="phone-screen">
            <div class="phone-header-bar"></div>
            <div class="phone-card-lg"></div>
            <div class="phone-card-row">
              <div class="phone-card-sm"></div>
              <div class="phone-card-sm"></div>
            </div>
            <div class="phone-card-lg phone-card-lg--xs"></div>
          </div>
        </div>
      </div>

      <div class="app-content">
        <h2 id="app-h2" class="app-h2">Un'app che ti accompagna in giro.</h2>
        <p class="app-sub">
          Accedi ai drink dell'evento, raccogli i tuoi punti e riscatta i premi dei partner.
        </p>

        <div class="app-cards">
          {appFeatures.map(({ title, desc, wide }) => (
            <div class:list={['app-card', { 'app-card--wide': wide }]}>
              <h3 class="app-card-title">{title}</h3>
              <p class="app-card-desc">{desc}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- S07 · VOCI DALLA LISTA ─────────────────────────────────── -->
  <section class="s-quotes" aria-labelledby="quotes-h2">
    <div class="wrap quotes-header">
      <h2 id="quotes-h2" class="quotes-h2">Cosa dice chi si è già registrato</h2>
      <p class="quotes-counter" aria-live="polite">
        <strong class="counter-num" id="counter-quotes">347</strong> persone già in lista
      </p>
    </div>

    <div class="quotes-outer">
      <div class="quotes-track" id="quotes-track" role="list">
        {quotes.map(({ text, name, age }) => (
          <article class="quote-card" role="listitem">
            <blockquote class="quote-text">"{text}"</blockquote>
            <footer class="quote-author">— {name}, {age}</footer>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- S08 · CTA FINALE ───────────────────────────────────────── -->
  <section class="s-cta-final" aria-labelledby="cta-final-h2">
    <div class="wrap cta-final-inner">
      <h2 id="cta-final-h2" class="cta-final-h2">
        Milano. Maggio 2027.<br />Sii il primo a sapere quando escono i biglietti.
      </h2>
      <a href="#lista" class="btn btn--final">
        Entra in lista d'attesa <span aria-hidden="true">✶</span>
      </a>
      <p class="cta-trust">Ti scriviamo solo quando aprono i pass. Niente spam, parola.</p>
    </div>
  </section>

  <!-- S09 · FAQ ──────────────────────────────────────────────── -->
  <section class="s-faq" aria-labelledby="faq-h2">
    <div class="wrap">
      <h2 id="faq-h2" class="faq-h2">Domande, in due righe.</h2>
      <div class="faq-list" id="faq-list">
        {faqs.map(({ q, a }) => (
          <details class="faq-item">
            <summary class="faq-q">
              {q}
              <span class="faq-icon" aria-hidden="true">✶</span>
            </summary>
            <p class="faq-a">{a}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- FORM ───────────────────────────────────────────────────── -->
  <section class="s-lista" id="lista" aria-labelledby="lista-h2">
    <div class="wrap">
      <h2 id="lista-h2" class="lista-h2">
        Lista d'attesa <span aria-hidden="true">✶</span>
      </h2>
      <p class="lista-intro">
        Iscriviti gratuitamente. Ti contatteremo per primo all'apertura ufficiale dei pass.
      </p>
      <div class="tally-wrap">
        <iframe
          data-tally-src="https://tally.so/embed/Y5pkp5?alignLeft=1&hideTitle=1&transparentBackground=1&dynamicHeight=1&hideFooter=1"
          loading="lazy"
          width="100%"
          height="689"
          title="Milan Cocktail Days — Lista d'attesa partecipanti"
        ></iframe>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  .wrap {
    max-width: var(--container);
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* ── HERO ─────────────────────────────────────────────────── */
  .s-hero {
    position: relative;
    min-height: 80svh;
    display: flex;
    align-items: center;
    padding: clamp(2rem, 5vw, 4rem) 0;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-bg img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 80% center;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: linear-gradient(
      to right,
      color-mix(in srgb, var(--color-inchiostro) 55%, transparent) 0%,
      color-mix(in srgb, var(--color-inchiostro) 15%, transparent) 30%,
      transparent 60%
    );
  }

  .hero-inner {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-sm);
    max-width: 42%;
    margin-left: 100px;
    padding: var(--space-lg);
    background: color-mix(in srgb, var(--color-panna) 8%, transparent);
    backdrop-filter: blur(10px);
    border: 1px solid color-mix(in srgb, var(--color-panna) 20%, transparent);
    border-radius: var(--radius-md);
  }

  .hero-overline {
    font-family: var(--font-body);
    font-size: var(--text-label);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.22em;
    color: var(--color-panna);
    margin: 0;
  }

  .hero-h1-wrap {
    padding: var(--space-sm) var(--space-md);
  }

  .hero-h1 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: clamp(2rem, 4.5vw, 4rem);
    color: var(--color-panna);
    line-height: 1.05;
    letter-spacing: -0.01em;
    margin: 0;
  }

  .hero-sub {
    font-family: var(--font-body);
    font-size: var(--text-body-lg);
    color: var(--color-pesco-300);
    line-height: 1.6;
    margin: 0;
    max-width: 480px;
  }

  /* ── SHARED BTN ───────────────────────────────────────────── */
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4em;
    padding: 0.875rem 2rem;
    font-family: var(--font-body);
    font-size: var(--text-small);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    text-decoration: none;
    border-radius: var(--radius-sm);
    transition: background-color 0.2s ease, border-color 0.2s ease;
    margin-top: var(--space-sm);
  }

  .btn--primary {
    background-color: var(--color-bitter);
    color: var(--color-panna);
    border: 1px solid var(--color-bitter);
  }

  .btn--primary:hover {
    background-color: var(--color-bitter-500);
    border-color: var(--color-bitter-500);
  }

  .btn--final {
    background-color: var(--color-bitter);
    color: var(--color-panna);
    border: 1px solid var(--color-bitter);
    font-size: var(--text-body);
    padding: 1rem 2.5rem;
    margin-top: 0;
  }

  .btn--final:hover {
    background-color: var(--color-bitter-500);
    border-color: var(--color-bitter-500);
  }

  /* ── COME FUNZIONA ────────────────────────────────────────── */
  .s-how {
    background-color: var(--color-fumo);
    padding: clamp(2rem, 5vw, 4rem) 0;
  }

  .how-h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h1);
    color: var(--color-panna);
    line-height: 1.05;
    margin: 0 0 var(--space-md);
  }

  .step-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
  }

  .step {
    padding: var(--space-md) 0;
    border-top: var(--border-hairline-dark);
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .step-header {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
  }

  .step-n {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--color-bitter);
    line-height: 1;
    flex-shrink: 0;
    letter-spacing: -0.03em;
    min-width: 64px;
  }

  .step-bullet {
    color: var(--color-bitter);
    font-size: 0.75rem;
    flex-shrink: 0;
  }

  .step-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-body-lg);
    color: var(--color-panna);
    letter-spacing: 0.02em;
    margin: 0;
  }

  .step-desc {
    font-family: var(--font-body);
    font-size: var(--text-body);
    color: var(--color-pesco-300);
    line-height: 1.7;
    margin: 0;
    padding-left: calc(64px + var(--space-sm) + 0.75rem + var(--space-sm));
    max-width: 680px;
  }

  .how-strap {
    margin-top: var(--space-lg);
    padding-top: var(--space-sm);
    border-top: var(--border-hairline-dark);
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-h2);
    color: var(--color-panna);
    line-height: 1.2;
  }

  /* ── PERCHÉ ISCRIVERSI ────────────────────────────────────── */
  .s-why {
    background-color: var(--color-avorio);
    padding: clamp(2rem, 5vw, 4rem) 0;
  }

  .why-h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h1);
    color: var(--color-mora);
    line-height: 1.05;
    margin: 0 0 var(--space-md);
  }

  .why-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-lg);
    margin-bottom: var(--space-md);
  }

  .why-block-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-h2);
    color: var(--color-mora);
    margin: 0 0 var(--space-sm);
  }

  .why-block-body {
    font-family: var(--font-body);
    font-size: var(--text-body);
    color: var(--color-inchiostro);
    line-height: 1.75;
    margin: 0;
  }

  .price {
    font-family: 'Courier New', Courier, monospace;
    font-weight: 700;
    letter-spacing: 0.08em;
    color: var(--color-bitter);
  }

  .why-counter {
    font-family: var(--font-body);
    font-size: var(--text-body);
    color: var(--color-inchiostro-500);
    margin: 0 0 var(--space-sm);
  }

  .counter-num {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h1);
    color: var(--color-basilico);
    display: inline;
  }

  /* ── PER CHI È ────────────────────────────────────────────── */
  .s-perchi {
    background-color: var(--color-basilico);
    padding: clamp(2rem, 5vw, 4rem) 0;
  }

  .perchi-h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h1);
    color: var(--color-panna);
    line-height: 1.05;
    margin: 0 0 var(--space-md);
    max-width: 760px;
  }

  .perchi-lead {
    font-family: var(--font-body);
    font-size: var(--text-body-lg);
    color: var(--color-pesco-300);
    line-height: 1.7;
    margin: 0 0 var(--space-md);
    max-width: 640px;
  }

  .perchi-occasions {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-md);
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-sm);
  }

  .perchi-occasions li {
    font-family: var(--font-body);
    font-size: var(--text-body);
    color: var(--color-panna);
    font-style: italic;
    line-height: 1.5;
    padding: var(--space-md);
    border: 0.5px solid color-mix(in srgb, var(--color-panna) 20%, transparent);
    border-radius: var(--radius-sm);
    background-color: var(--color-basilico-600);
  }

  .perchi-close {
    font-family: var(--font-body);
    font-size: var(--text-body);
    color: var(--color-panna);
    opacity: 0.7;
    margin: 0;
    font-style: italic;
  }

  /* ── BAR PARTNER ──────────────────────────────────────────── */
  .s-bars {
    background-color: var(--color-avorio);
    padding: clamp(2rem, 5vw, 4rem) 0;
  }

  .bars-h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h1);
    color: var(--color-mora);
    line-height: 1.05;
    margin: 0 0 var(--space-sm);
  }

  .bars-lead {
    font-family: var(--font-body);
    font-size: var(--text-body);
    color: var(--color-inchiostro);
    line-height: 1.75;
    margin: 0 0 var(--space-md);
    max-width: 640px;
  }

  .criteria-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md);
    margin-bottom: var(--space-md);
  }

  .criterion {
    padding: var(--space-md) 0 var(--space-md) var(--space-sm);
    border-top: 2px solid var(--color-basilico);
  }

  .criterion-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-body);
    color: var(--color-mora);
    margin: 0 0 var(--space-xs);
  }

  .criterion-desc {
    font-family: var(--font-body);
    font-size: var(--text-small);
    color: var(--color-inchiostro-500);
    line-height: 1.65;
    margin: 0;
  }

  .map-wrap {
    margin: 0 0 var(--space-md);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
  }

  .milan-map {
    width: 100%;
    max-width: 480px;
    color: var(--color-basilico);
  }

  .map-caption {
    font-family: var(--font-body);
    font-size: var(--text-label);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-inchiostro-500);
    text-align: center;
  }

  /* ── APP ──────────────────────────────────────────────────── */
  .s-app {
    background-color: var(--color-fumo);
    padding: clamp(2rem, 5vw, 4rem) 0;
  }

  .app-inner {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: var(--space-lg);
    align-items: center;
  }

  .app-mock {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .phone-frame {
    width: 240px;
    height: 480px;
    border: 1.5px solid color-mix(in srgb, var(--color-panna) 20%, transparent);
    border-radius: 28px;
    background-color: var(--color-marmo);
    padding: 16px 12px 20px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .phone-notch {
    width: 60px;
    height: 8px;
    background-color: color-mix(in srgb, var(--color-panna) 15%, transparent);
    border-radius: 4px;
    margin: 0 auto 6px;
  }

  .phone-screen {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .phone-header-bar {
    height: 24px;
    background-color: color-mix(in srgb, var(--color-basilico) 40%, transparent);
    border-radius: var(--radius-sm);
  }

  .phone-card-lg {
    flex: 1;
    background-color: color-mix(in srgb, var(--color-bitter) 30%, transparent);
    border-radius: var(--radius-sm);
    border: 0.5px solid color-mix(in srgb, var(--color-panna) 15%, transparent);
  }

  .phone-card-lg--xs {
    flex: 0 0 40px;
    background-color: color-mix(in srgb, var(--color-basilico) 20%, transparent);
  }

  .phone-card-row { display: flex; gap: 8px; height: 60px; }

  .phone-card-sm {
    flex: 1;
    background-color: color-mix(in srgb, var(--color-panna) 10%, transparent);
    border-radius: var(--radius-sm);
  }

  .app-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .app-h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h1);
    color: var(--color-panna);
    line-height: 1.05;
    margin: 0;
  }

  .app-sub {
    font-family: var(--font-body);
    font-size: var(--text-body);
    color: var(--color-pesco-300);
    line-height: 1.6;
    margin: 0;
    opacity: 0.85;
  }

  .app-cards {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-sm);
  }

  .app-card {
    background-color: color-mix(in srgb, var(--color-panna) 6%, transparent);
    border: 0.5px solid color-mix(in srgb, var(--color-panna) 15%, transparent);
    border-radius: var(--radius-sm);
    padding: var(--space-md);
  }

  .app-card--wide {
    grid-column: 1 / -1;
  }

  .app-card-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-small);
    color: var(--color-panna);
    margin: 0 0 var(--space-xs);
    letter-spacing: 0.02em;
  }

  .app-card-desc {
    font-family: var(--font-body);
    font-size: var(--text-small);
    color: var(--color-pesco-300);
    line-height: 1.65;
    margin: 0;
    opacity: 0.85;
  }

  /* ── VOCI DALLA LISTA ─────────────────────────────────────── */
  .s-quotes {
    background-color: var(--color-panna-100);
    padding: clamp(2rem, 5vw, 4rem) 0;
    overflow: hidden;
  }

  .quotes-header {
    margin-bottom: var(--space-md);
    display: flex;
    align-items: baseline;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  .quotes-h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h2);
    color: var(--color-mora);
    margin: 0;
  }

  .quotes-counter {
    font-family: var(--font-body);
    font-size: var(--text-small);
    color: var(--color-inchiostro-500);
    margin: 0;
  }

  .quotes-counter .counter-num {
    font-size: var(--text-body);
    color: var(--color-basilico);
  }

  .quotes-outer {
    overflow-x: auto;
    padding: 0 2rem var(--space-sm);
    scrollbar-width: none;
  }

  .quotes-outer::-webkit-scrollbar { display: none; }

  .quotes-track {
    display: flex;
    gap: var(--space-md);
    width: max-content;
    scroll-snap-type: x mandatory;
  }

  .quote-card {
    scroll-snap-align: start;
    flex: 0 0 min(320px, 80vw);
    background-color: var(--color-panna);
    border: 0.5px solid var(--color-panna-300);
    border-radius: var(--radius-md);
    padding: var(--space-md);
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .quote-text {
    font-family: var(--font-body);
    font-style: italic;
    font-size: var(--text-body);
    color: var(--color-mora);
    line-height: 1.65;
    quotes: none;
    margin: 0;
  }

  .quote-author {
    font-family: var(--font-body);
    font-size: var(--text-label);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-inchiostro-700);
  }

  /* ── CTA FINALE ───────────────────────────────────────────── */
  .s-cta-final {
    background-color: var(--color-mora);
    padding: clamp(2rem, 5vw, 4rem) 0;
    border-top: 0.5px solid color-mix(in srgb, var(--color-panna) 10%, transparent);
  }

  .cta-final-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-md);
  }

  .cta-final-h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h1);
    color: var(--color-panna);
    line-height: 1.1;
    margin: 0;
  }

  .cta-trust {
    font-family: var(--font-body);
    font-size: var(--text-small);
    color: var(--color-pesco-300);
    opacity: 0.75;
    margin: 0;
  }

  /* ── FAQ ──────────────────────────────────────────────────── */
  .s-faq {
    background-color: var(--color-avorio);
    padding: clamp(2rem, 5vw, 4rem) 0;
  }

  .faq-h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h1);
    color: var(--color-mora);
    line-height: 1.05;
    margin: 0 0 var(--space-md);
  }

  .faq-list {
    max-width: 760px;
    display: flex;
    flex-direction: column;
  }

  .faq-item {
    border-top: 0.5px solid var(--color-panna-300);
  }

  .faq-item:last-child {
    border-bottom: 0.5px solid var(--color-panna-300);
  }

  .faq-q {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-md) 0;
    cursor: pointer;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-body);
    color: var(--color-mora);
    list-style: none;
    user-select: none;
  }

  .faq-q::-webkit-details-marker { display: none; }

  .faq-icon {
    color: var(--color-bitter);
    flex-shrink: 0;
    transition: transform 0.25s ease;
    font-size: 0.9em;
  }

  details[open] .faq-icon {
    transform: rotate(45deg);
  }

  .faq-a {
    font-family: var(--font-body);
    font-size: var(--text-body);
    color: var(--color-inchiostro);
    line-height: 1.75;
    margin: 0;
    padding: 0 var(--space-lg) var(--space-md) 0;
  }

  /* ── FORM ─────────────────────────────────────────────────── */
  .s-lista {
    background-color: var(--color-fumo);
    padding: clamp(2rem, 5vw, 4rem) 0;
    scroll-margin-top: 64px;
  }

  .lista-h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: var(--text-h1);
    color: var(--color-panna);
    line-height: 1.05;
    margin: 0 0 var(--space-sm);
  }

  .lista-h2 span {
    font-size: 0.5em;
    vertical-align: middle;
    opacity: 0.5;
  }

  .lista-intro {
    font-family: var(--font-body);
    font-size: var(--text-body);
    color: var(--color-pesco-300);
    line-height: 1.65;
    max-width: 560px;
    margin: 0 0 var(--space-lg);
    opacity: 0.85;
  }

  .tally-wrap { max-width: 760px; }

  /* ── RESPONSIVE ───────────────────────────────────────────── */
  @media (max-width: 900px) {
    .why-grid        { grid-template-columns: 1fr; }
    .criteria-grid   { grid-template-columns: 1fr; }
    .perchi-occasions { grid-template-columns: 1fr; }
    .app-inner       { grid-template-columns: 1fr; }
    .app-mock        { display: none; }
  }

  @media (max-width: 768px) {
    .hero-inner {
      max-width: 100%;
      margin-left: 0;
    }
    .hero-overlay {
      background: var(--color-panna);
      opacity: 0.92;
    }
    .hero-h1         { font-size: clamp(1.75rem, 8vw, 3rem); }
    .step-desc       { padding-left: 0; }
    .quotes-header   { flex-direction: column; gap: var(--space-xs); }
    .app-cards       { grid-template-columns: 1fr; }
    .app-card--wide  { grid-column: auto; }
    .cta-final-h2    { font-size: var(--text-h2); }
  }
</style>

<script>
  // FAQ accordion: close others on open
  document.querySelectorAll('.faq-item').forEach(detail => {
    detail.addEventListener('toggle', () => {
      if ((detail as HTMLDetailsElement).open) {
        document.querySelectorAll('.faq-item').forEach(other => {
          if (other !== detail) (other as HTMLDetailsElement).open = false;
        });
      }
    });
  });

  // Quote carousel: auto-advance every 4s
  const quotesOuter = document.querySelector('.quotes-outer');
  const quotesTrack = document.getElementById('quotes-track');
  if (quotesOuter && quotesTrack) {
    const cards = Array.from(quotesTrack.querySelectorAll('.quote-card')) as HTMLElement[];
    let idx = 0;
    setInterval(() => {
      idx = (idx + 1) % cards.length;
      (quotesOuter as HTMLElement).scrollTo({
        left: cards[idx].offsetLeft,
        behavior: 'smooth',
      });
    }, 4000);
  }
</script>
